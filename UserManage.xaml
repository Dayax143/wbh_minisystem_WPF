<UserControl x:Class="fortest.UserManage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Label Content="User Id" Margin="5"/>
            <ComboBox Grid.Column="1" Margin="5"/>
            <Button Grid.Column="2" Content="Refresh" Margin="5"/>
        </Grid>

        <Border Grid.Row="1" BorderBrush="LightGray" BorderThickness="1" Padding="10" Margin="0,10,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Label Grid.Row="0" Grid.Column="0" Content="Username" Margin="5"/>
                <TextBox Grid.Row="0" Grid.Column="1" Margin="5"/>
                <Label Grid.Row="1" Grid.Column="0" Content="Password" Margin="5"/>
                <TextBox Grid.Row="1" Grid.Column="1" Margin="5"/>
                <Label Grid.Row="2" Grid.Column="0" Content="Recovery question" Margin="5"/>
                <ComboBox Grid.Row="2" Grid.Column="1" Margin="5"/>
                <Label Grid.Row="3" Grid.Column="0" Content="Recovery answer" Margin="5"/>
                <TextBox Grid.Row="3" Grid.Column="1" Margin="5"/>

                <Label Grid.Row="0" Grid.Column="2" Content="Usertype" Margin="5"/>
                <ComboBox Grid.Row="0" Grid.Column="3" Margin="5"/>
                <Label Grid.Row="1" Grid.Column="2" Content="User status" Margin="5"/>
                <ComboBox Grid.Row="1" Grid.Column="3" Margin="5"/>

                <StackPanel Grid.Row="0" Grid.RowSpan="2" Grid.Column="3" Orientation="Vertical" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="5">
                    <Button Content="Save" Margin="5,0"/>
                    <Button Content="Update" Margin="5,0"/>
                    <Button Content="Delete" Margin="5,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Orientation="Horizontal" Grid.Row="0">
                <Label Content="Search" Margin="5"/>
                <TextBox Width="200" Margin="5"/>
                <Label Content="View" Margin="10,0,0,0"/>
            </StackPanel>

            <DataGrid Grid.Row="1" AutoGenerateColumns="False" Margin="0,10,0,0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="User_id" Binding="{Binding UserId}" Width="Auto"/>
                    <DataGridTextColumn Header="username" Binding="{Binding Username}" Width="*"/>
                    <DataGridTextColumn Header="password" Binding="{Binding Password}" Width="*"/>
                    <DataGridTextColumn Header="usertype" Binding="{Binding Usertype}" Width="*"/>
                    <DataGridTextColumn Header="Recovery question" Binding="{Binding RecoveryQuestion}" Width="*"/>
                    <DataGridTextColumn Header="Recovery answer" Binding="{Binding RecoveryAnswer}" Width="*"/>
                    <DataGridTextColumn Header="userStatus" Binding="{Binding UserStatus}" Width="*"/>
                    <DataGridTextColumn Header="date" Binding="{Binding Date}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</UserControl>