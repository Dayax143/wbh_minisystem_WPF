<UserControl x:Class="fortest.PlatesManagement"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:fortest"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Foreground="#333333"
    d:DesignWidth="1360" d:DesignHeight="768">
    <Grid Margin="0,0,19,6">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="1" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="Search" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBox Grid.Column="1" Margin="0,0,10,0"/>
                <Button Grid.Column="2" Content="View" Margin="0,0,5,0"/>
                <Button Grid.Column="4" Content="Refresh" HorizontalAlignment="Right"/>
            </Grid>

            <DataGrid Grid.Row="1" AutoGenerateColumns="True" CanUserAddRows="False" Name="dgvPlate">
                <DataGrid.Columns>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</UserControl>