<Window x:Class="fortest.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:fortest"
        mc:Ignorable="d"
        Title="Dashboard" Height="768" Width="1366">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top Header -->
        <Border Grid.ColumnSpan="2" Background="#007ACC" >
            <Grid>
                <TextBlock Text="Dashboard" Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="10,0,0,0" FontSize="18" FontWeight="Bold"/>
                <TextBlock Text="WELCOME HWA kempower interfaces" Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="18" FontWeight="Bold"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <TextBlock Text="07/30/2025 5:38:01 AM" Foreground="White" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <Button Content="Logout" Background="#FF4500" Foreground="White" Padding="10,5" Margin="0,0,10,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Left Navigation Pane -->
        <Border Grid.Row="1" Background="#1E90FF">
            <StackPanel Margin="0,20,0,0">
                <Button Content="Dashboard" />
                <Button Content="Manage users" />
                <Button Content="Settings" />
                <Button Content="About" />
                <Button Background="Azure" Foreground="Yellow" Margin="10">content</Button>
            </StackPanel>
        </Border>

        <!-- Main Content Area -->
        <Border Grid.Column="1" Grid.Row="1" Background="#F0F0F0" Margin="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Tab Control -->
                <TabControl Grid.Row="0" Margin="0,0,0,10">
                    <TabItem Header="Lora">
                        <Grid Margin="10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Welcome to Lora Reference Management" FontSize="16" FontWeight="Bold" Margin="0,0,0,10" Grid.ColumnSpan="2"/>

                            <TextBlock Grid.Row="1" Grid.Column="0" Text="Lora_Id" Margin="0,5,10,5"/>
                            <TextBox Grid.Row="1" Grid.Column="1" Width="200" HorizontalAlignment="Left" Margin="0,5,0,5"/>

                            <TextBlock Grid.Row="2" Grid.Column="0" Text="Lora_Supply" Margin="0,5,10,5"/>
                            <TextBox Grid.Row="2" Grid.Column="1" Width="200" HorizontalAlignment="Left" Margin="0,5,0,5"/>

                            <TextBlock Grid.Row="3" Grid.Column="0" Text="Reference" Margin="0,5,10,5"/>
                            <TextBox Grid.Row="3" Grid.Column="1" Width="200" HorizontalAlignment="Left" Margin="0,5,0,5"/>
                            <CheckBox Grid.Row="3" Grid.Column="1" Content="Taken" HorizontalAlignment="Left" Margin="210,5,0,5"/>

                            <TextBlock Grid.Row="4" Grid.Column="0" Text="Serial" Margin="0,5,10,5"/>
                            <TextBox Grid.Row="4" Grid.Column="1" Width="200" HorizontalAlignment="Left" Margin="0,5,0,5"/>

                            <TextBlock Grid.Row="5" Grid.Column="0" Text="RV" Margin="0,5,10,5"/>
                            <TextBox Grid.Row="5" Grid.Column="1" Width="200" HorizontalAlignment="Left" Margin="0,5,0,5"/>
                            <CheckBox Grid.Row="5" Grid.Column="1" Content="Plate" HorizontalAlignment="Left" Margin="210,5,0,5"/>

                            <TextBlock Grid.Row="6" Grid.Column="0" Text="Note :" Margin="0,5,10,5"/>
                            <TextBox Grid.Row="6" Grid.Column="1" Width="200" Height="50" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" HorizontalAlignment="Left" Margin="0,5,0,5"/>

                            <!-- Buttons -->
                            <StackPanel Grid.Row="1" Grid.Column="2" Grid.RowSpan="6" VerticalAlignment="Top" Margin="20,0,0,0">
                                <Button Content="New" Width="80" Height="30" Margin="0,0,0,5"/>
                                <Button Content="Save" Width="80" Height="30" Margin="0,0,0,5"/>
                                <Button Content="Update" Width="80" Height="30" Margin="0,0,0,5"/>
                                <Button Content="Delete" Width="80" Height="30" Margin="0,0,0,5"/>
                            </StackPanel>
                            <Button Content="Refresh" Grid.Row="0" Grid.Column="2" HorizontalAlignment="Right" VerticalAlignment="Top" Width="80" Height="30"/>
                        </Grid>
                    </TabItem>
                    <TabItem Header="Plates" Margin="-2,-2,-2,2"/>
                    <TabItem Header="RFI" HorizontalAlignment="Left" Margin="-2,-2,0,0" Width="32"/>
                    <TabItem Header="M Reader" Height="22" Margin="-2,-2,-2,0" VerticalAlignment="Top"/>
                </TabControl>

                <!-- Search and Data Grid -->
                <Grid Grid.Row="1" Margin="0,10,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Search" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <TextBox Grid.Row="0" Grid.Column="1" Width="200" HorizontalAlignment="Left" Margin="0,0,0,0"/>

                    <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Left" Margin="210,0,0,0">
                        <Button Content="Today" Width="60" Height="25" Margin="0,0,5,0"/>
                        <Button Content="Pending" Width="60" Height="25"/>
                    </StackPanel>
                    <Button Content="Report" Grid.Row="0" Grid.Column="2" HorizontalAlignment="Right" Width="80" Height="25"/>
                </Grid>

                <DataGrid Grid.Row="2" Margin="0,10,0,0" AutoGenerateColumns="True" CanUserAddRows="False">
                    <!-- DataGrid Columns would be defined here based on your data source -->
                    <!-- Example for a few columns, you would add more as needed -->
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="lora_id" Binding="{Binding LoraId}"/>
                        <DataGridTextColumn Header="cor_supply" Binding="{Binding CorSupply}"/>
                        <DataGridTextColumn Header="Status" Binding="{Binding Status}"/>
                        <DataGridTextColumn Header="reference" Binding="{Binding Reference}"/>
                        <DataGridTextColumn Header="taken_date" Binding="{Binding TakenDate}"/>
                        <DataGridTextColumn Header="received_id" Binding="{Binding ReceivedId}"/>
                        <DataGridTextColumn Header="lora_serial" Binding="{Binding LoraSerial}"/>
                        <DataGridTextColumn Header="View" Binding="{Binding View}"/>
                        <DataGridTextColumn Header="note" Binding="{Binding Note}"/>
                        <DataGridTextColumn Header="users" Binding="{Binding Users}"/>
                        <DataGridTextColumn Header="assigned_to" Binding="{Binding AssignedTo}"/>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- Activate Windows watermark - this is typically not part of the app's UI but a system overlay -->
                <TextBlock Grid.Row="2" Grid.ColumnSpan="2" Text="Activate Windows&#x0a;Go to Settings to activate Windows." HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,10,10" Foreground="Gray" FontSize="12" TextAlignment="Right"/>

            </Grid>
        </Border>
    </Grid>

    <Window.Resources>
        <Style x:Key="NavButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#1E90FF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Margin" Value="0,5,0,5"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#007ACC"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#005691"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
</Window>